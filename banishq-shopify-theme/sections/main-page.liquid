<div class="page-width page-width--narrow section">
  {% render 'breadcrumbs' %}
  <h1 class="h-serif h1" style="margin-bottom: 26px">{{ page.title }}</h1>
  <div class="rte body-lg">{{ page.content }}</div>
</div>
{% schema %}
{ "name": "Page" }
{% endschema %}
